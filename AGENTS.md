# 本项目协作要求

优先使用 Superpowers 中与当前任务直接相关的技能，并遵循用户已确认的 PRD；用户明确要求直接执行时，不重复设计或询问已确定事项。

## 低额度执行模式

- 优先复用已有上下文，以 Git diff 和本次需求为边界，只读取直接相关文件及必要依赖。
- 禁止无关全仓扫描、重复分析、重复测试和额外重构。
- 采用最小修改和最小相关验证，确认需求正确完成后立即停止。
- 只有发现明确跨模块影响或正确性风险时才扩大范围。
- 最终简洁汇报修改、验证结果和遗留问题。

## 开发约束

- 所有面向玩家的文案使用简体中文，统一中文映射；不得泄漏内部字段或空值。
- 原始属性不限制在 0～100；概率使用统一软饱和工具。
- 状态变化统一经过效果引擎；长期后果经过记忆引擎。
- 事件优先数据驱动，不增加 PRD 未要求的大型系统，不在运行时调用 AI API。
- 保留 GitHub 作为主代码仓库。功能修改放入独立分支，通过 PR 审阅；不擅自合并 main。
- 先跑通核心循环与现有七条测试路线，再扩充事件库。
- `dist/` 是被跟踪的原生网页源码，不是可随意清除的构建输出。
