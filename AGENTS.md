# Codex 开发约束

## 项目
中文网页人生模拟器。

## 默认工作方式
- 日常开发基于 `develop`，不要直接改 `main`。
- 完成一个明确功能后再提交，避免无关大改。
- 优先读取 `docs/PRD.md`，以其为产品基准。
- 游戏界面、按钮、提示与内容默认使用中文。
- 优先低依赖、简单结构，不为未来需求过度设计。

## 架构原则
- 游戏规则与 UI 分离。
- 事件、职业、城市、爱好尽量配置化/数据驱动。
- 数值规则集中管理。
- 随机逻辑支持固定 seed，方便复现测试。
- 新功能尽量补基础测试，尤其是事件条件、属性结算和随机逻辑。

## MVP 边界
当前优先做单机网页版本；除非 PRD 更新，不做登录、服务器、排行榜、付费或社交系统。

## 修改要求
- 修改前先检查相关文件，避免重复实现。
- 不删除已有玩法，除非需求明确要求。
- 大改前先给出简短实施计划。
- 完成后运行现有测试/构建，并报告结果。
