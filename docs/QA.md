# 2026-09-09 试玩验收

本次只检查运行界面和存档链路，未扩展剧情或重构引擎。

## 通过

- 创建郑州测试角色，正常出生、写入 IndexedDB。
- 连续推进到 15 岁：普通年份直接推进，升学节点出现选择。
- 10 岁刷新后恢复相同姓名、年龄、属性和年度事件。
- 15 岁待选状态刷新后仍显示相同关键事件和选项。
- 选择高中后倒流回选择前，再选择职业教育。
- 再次刷新后保留新分支，倒流按钮仍为已用且禁用。
- 390 像素容器、375 像素有效内容视口中，页面宽度等于视口宽度，无整体横向溢出；菜单可横向滚动。
- 窄屏正常推进到 16 岁，五个人生愿望选项正常出现。

## 修复

- 非安全来源不支持 `crypto.randomUUID()`，导致 HTTP 开发环境创建失败。改用同样来自 Web Crypto 的随机字节生成出生种子；不改变既有种子的随机算法。
- 为现有原生开发服务器增加 `dev` 命令和端口参数支持，不增加依赖。

## 限制

以上为桌面 Chrome 和嵌入窄视口验证，未代表手机真机、Safari 或跨设备存档验收。测试角色与正式网站存档隔离。

## 第二轮：人生结局闭环

- 接续第一轮的 16 岁角色，通过正常界面逐年推进至 77 岁死亡，没有修改存档或跳过引擎。
- 经历创业、创业失败后转行、42 岁迁居深圳、购房、退休和严重疾病，年度流程没有阻塞。
- 完整履历包含 0～77 岁连续 78 条记录；报告与履历未出现所检查的 `undefined`、`null`、`NaN`、`health` 或 `careerLevel` 字段。
- 死亡后刷新并继续人生，报告正文与刷新前完全一致；历史人生入口打开相同报告。
- 新建“重开验收”角色后刷新，旧人生报告仍完整保留，新人物可继续且从 0 岁开始。
- 本轮未发现需要修改游戏代码的问题，只补充验收记录；未重复运行此前已通过且代码未变的引擎测试。
- 本轮只覆盖一局的正常界面结局闭环，不代表全部分支或手机真机已验收。
