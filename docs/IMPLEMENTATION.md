# 中国城市文字人生 V0.1 实施计划

需求权威：本次会话已确认的 84 节 PRD。现有仓库仅 README，无代码或依赖。

约束：简体中文；原始属性不限幅；状态变化统一效果引擎；六类长期后果统一记忆引擎；年度顺序固定；无运行时 AI/网络行情；手机优先；本地完整存档；80～120 条首批事件。

- [x] P0：data、state、probability、effect、memory、year；先测试固定种子、无限属性、年度完整性、条件与冷却、迁移、跨年记忆。
- [x] P1～P3：城市与 21 职业数据、教育/家庭/NPC、财富/住房/投资、创业/赌博、100 条左右事件；七条指定路线测试。
- [x] P4：中文界面、IndexedDB、检查点与一次回退、历史报告；完整人生抽样与资源验证。
- [ ] 交付：开发分支与草稿 PR，保留 main；报告实际验证结果及限制。

模块接口：createPlayer(options) 创建状态；applyEffects(state,effects,source) 返回新状态；advanceYear(state) 返回待选或年末状态；choose(state,index) 结束关键年份；rewind(state,checkpoint) 回退且保留已使用标记；getReport(state) 生成中文报告。

规则补充：财富金额统一人民币元；展示总资产采用资产减负债净值。未成年由父母承担基本支出。所有商业和城市数值是游戏平衡参数。死亡在主事件后年度关闭时判断。退休默认 60 岁，仅为游戏规则。事件概率软饱和，赌博不可正期望。使用独立开发分支，远程通过已授权 GitHub 连接保存。
